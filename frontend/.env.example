# Copy this file to .env.local and fill in your values
# NEXT_PUBLIC_ variables are exposed to the browser

NEXT_PUBLIC_API_URL=http://localhost:8000    # Backend URL — update to production URL when deploying
